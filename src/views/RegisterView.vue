<template>
  <section id="form">
    <!--form-->
    <div class="container">
      <div class="row">
        <!--sign up form-->
        <div class="col-sm-6">
          <div class="signup-form">
            <h2>التسجيل كمستخدم جديد!</h2>
            <form action="#">
              <input type="text" placeholder="اسم المستخدم" />
              <input type="email" placeholder="عنوان البريد الإلكتروني" />
              <input type="password" placeholder="كلمة المرور" />
              <button type="submit" class="btn btn-default">تسجيل</button>
            </form>
          </div>
        </div>
        <!--/sign up form-->
      </div>
    </div>
  </section>
  <!--/form-->
</template>
<script>
export default {
  name: "RegisterView",
  created() {
    if (localStorage.productToShow) {
      localStorage.removeItem("productToShow");
      console.log("removed productToShow - view:register");
    }
    if (localStorage.productToUpdate) {
      localStorage.removeItem("productToUpdate");
      console.log("removed productToUpdate - view:show");
    }
  },
};
</script>
